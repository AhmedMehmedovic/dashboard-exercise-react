import React from "react";
import { useTitle } from "react-use";

function ColorMapping() {
  useTitle("Color Mapping");
  return <div>Have to add code for charts</div>;
}

export default ColorMapping;
